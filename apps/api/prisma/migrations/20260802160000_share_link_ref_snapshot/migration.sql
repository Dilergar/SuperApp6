-- Снимок названия объекта на момент выдачи ссылки.
--
-- Без него раздел «Ссылки наружу» звал резолвер потребителя на КАЖДУЮ строку списка,
-- показывал «Объект удалён» вместо имени там, где объекта уже нет, и светил ТЕКУЩЕЕ имя
-- тому, у кого доступ к объекту давно отобрали. В списке «что я раздал» важно, чем это
-- было в момент раздачи, — снимок и честнее, и дешевле.
--
-- Старые строки остаются с NULL: для них список откатывается на describeRef.
ALTER TABLE "share_links" ADD COLUMN "ref_title" TEXT;
ALTER TABLE "share_links" ADD COLUMN "ref_icon" TEXT;
