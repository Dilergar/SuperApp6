-- Автоимя узла Диска: имя дала ПЛАТФОРМА, а не человек.
--
-- «ДОГ-2026-001 Договор (с печатью).pdf» — имя, которое собирает документооборот:
-- номер и название принадлежат организации (данные), а приписка «(с печатью)» —
-- слово продукта. Записанное целиком, оно застывало в языке источника, и
-- казахоязычный сотрудник видел английскую приписку в своём списке файлов.
--
-- Теперь рядом с именем лежит ключ каталога и его параметры: `name` остаётся
-- снимком языка-источника (по нему живут поиск, сортировка и уникальность в папке),
-- а человек читает имя, собранное при чтении на его языке. Переименовал сам —
-- ключ гасится, и дальше это его имя.
ALTER TABLE "drive_nodes" ADD COLUMN "auto_name_key" TEXT;
ALTER TABLE "drive_nodes" ADD COLUMN "auto_name_params" JSONB;
