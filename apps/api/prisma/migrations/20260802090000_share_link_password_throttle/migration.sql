-- Перебор пароля гостевой ссылки: счётчик неудач живёт на самой ссылке.
-- Троттлер платформы считает по IP, а у распределённого перебора адресов сколько
-- угодно — при минимуме в 4 символа четырёхзначный пароль подбирался за минуты.
-- Модель попыток — core/verify, но вместо гашения цепочки временная блокировка:
-- убить ссылку насмерть значит дать любому, кто знает токен, отрезать получателя.
ALTER TABLE "share_links" ADD COLUMN "pwd_failed_attempts" INTEGER NOT NULL DEFAULT 0;
ALTER TABLE "share_links" ADD COLUMN "pwd_locked_until" TIMESTAMP(3);
