-- Мультиязычность: язык по умолчанию — казахский (государственный язык РК).
-- Меняем ТОЛЬКО дефолт колонки: существующие строки хранят осознанный (или
-- унаследованный) выбор человека, и переписывать его миграцией нельзя.
ALTER TABLE "users" ALTER COLUMN "locale" SET DEFAULT 'kk';
