// ============================================================
// Имена джобов движка согласований — ОТДЕЛЬНЫМ файлом без импортов.
// ============================================================
// Сервис ставит джобы, обработчик их регистрирует, и обоим нужны одни и те же
// строки. Держать их в файле обработчика нельзя: он инжектит сервис (чтобы
// отложенное оповещение шло тем же кодом, что и обычное), и получалось кольцо
// ES-модулей — при загрузке один из двух оказывался `undefined`, а Nest падал
// на старте с «undefined dependency».

/** Срок подходит — напомнить адресатам, пока ещё можно успеть */
export const APPROVAL_REMIND_JOB = 'approvals.remind';
/** Срок решения вышел — напомнить адресатам и сказать автору */
export const APPROVAL_ESCALATE_JOB = 'approvals.escalate';
/** Заявка закрыта — разбудить того, кто ведёт её снаружи (процесс) */
export const APPROVAL_RESOLVED_JOB = 'approvals.resolved';
/**
 * Оповестить о текущем состоянии заявки ПОСЛЕ коммита ЧУЖОЙ транзакции.
 *
 * Обычный путь решения зовёт оповещение сам, сразу после своей транзакции. Но
 * решение, подтверждённое подписью (`decideAttested`), приходит ИЗНУТРИ
 * транзакции движка подписи, и «после коммита» там наступает не у нас: позвать
 * следующую группу оттуда напрямую значило бы разослать уведомления по
 * транзакции, которая ещё может откатиться.
 */
export const APPROVAL_ANNOUNCE_JOB = 'approvals.announce';
