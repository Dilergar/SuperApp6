// Граница ожидания сервиса (правило платформы: в корне app/ держать нельзя —
// вешает 404 на вечный спиннер; у каждого сервиса — своя, реэкспортом).
export { default } from '@/components/shell/RouteLoading';
