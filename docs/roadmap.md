# Дорожная карта и технический долг

> Статусный документ (единственный в docs/, где даты уместны). Детальный gap-анализ с чек-боксами — [gap_analysis_v2.md](gap_analysis_v2.md). Продуктовое видение — `/PRODUCT.md`.

## Этапы

1. **Сейчас: новые сервисы** на готовом веб-фундаменте (каждый — через грилл дизайна перед стройкой).
2. **Mobile** — подготовка (push-пайплайн, design-tokens, upload, OTP-экран) + переписать приложение целиком. `packages/api-client` уже построен. **Жёсткий блокер**: текущее mobile шлёт `register` без `verifyToken` — в production вернёт 400; OTP-экран строится в НОВОМ приложении.
3. **AI** — сервисные ассистенты + SuperAIAgent6 с голосом (архитектура готовится уже сейчас: тонкие контроллеры = будущие AI-инструменты).
4. **Терминал + Умный дом/IoT** — железо после AI-софта.

Фоном, не блокируя этапы: хвост прод-минимума и web-гигиена (ниже).

## Что построено (текущее состояние)

**Фундамент**: Circle (Окружение) · Задачник · Календарь (+Google) · Мессенджер · My Wish & Shop · Кошелёк-леджер · Организации · Сотрудники + **Орг. структура** (2026-09: вертикаль на графе должностей и объектов, заместители, областные права, канвас, мастер — [org_structure.md](org_structure.md)) · Скины карточек · Процессы (Ф1–Ф6 без RAG) · Финансы (B2C) · Диктофон · Виртуальный офис · Диск (OmniDrive).

**21 движок core/**: access · rich-cards · search · quick-actions · files · voice · calls · chatter · jobs · verify · docs · share-links · approvals · sign · templates · audiences (единый словарь адресатов, 2026-09) · notifications · realtime (2026-09) · entitlements (тарифы и лимиты — [entitlements_engine.md](entitlements_engine.md)) · platform (Кабинет платформы — [platform_console.md](platform_console.md)) (2026-09-12) · analytics (продуктовая аналитика в том же PostgreSQL: реестр событий, приём без БД на пути запроса, роллапы, дашборды и конструктор отчётов в Кабинете, SDK — [analytics_engine.md](analytics_engine.md)) (2026-09-13).

**Аналитика — дальше:** события остальных сервисов (по одному за подход: Диск, Заметки, Документооборот, Объекты…), витрина для админа организации (данные с `workspace_id` и k-анонимность готовы), уведомления о качестве данных, mobile-адаптер SDK (этап 2). **ClickHouse-тир — только по триггерам**: > 3000 событий/с, p95 дашборда > 1,5 с, > 15 % CPU БД на аналитике.

**Документная вертикаль ЗАВЕРШЕНА** (2026-08): Диск + share-links → core/templates (+блочный конструктор) → core/sign (ЭЦП НУЦ РК + ПЭП) → ЭДО (внешний контур «Документооборота» + «Контрагенты») → КЭДО (modules/hr: трудовые карточки, кадровые действия, библиотека 11 бланков РК, кампании ознакомления, ЕСУТД-очередь, «Мои документы»).

**B2B-вертикаль объектов ПОСТРОЕНА** (2026-09): юрлица организации (ТОО/ИП — сторона договора и владелец счетов) → дерево площадок с поясом и правилами смен → штатное расписание (вакансии, датированные назначения, версии ставок) → график смен (ротации, публикация, «Возьму», факт выходов и табель) → оборудование (модели, журналы движения и обслуживания) — [objects.md](objects.md), [legal_entities.md](legal_entities.md).

**Контрактная граница** закрыта тремя эшелонами (2026-08-07); **страж исходящих HTTP** построен (2026-08-30).

**Мультиязычный фундамент ПОСТРОЕН** (2026-09): пакет `@superapp/i18n` (kk/ru/en, ICU), язык запроса из `Accept-Language`, render-at-read у уведомлений, хроники и плашек чатов, отказы API кодом + переводом, переключатель языка, стражи (`no-cyrillic-literal` + ратчет, `pnpm check:i18n`) и переведённое ядро веба — [i18n.md](i18n.md). Дальше сервисы переводятся по одному за сессию ([i18n_migration.md](i18n_migration.md)).

## Ближайшие кандидаты

**Следующий шаг B2B-вертикали (порядок зафиксирован пользователем 2026-09) — «Доступы и видимость»**: первые четыре шага (Объекты → Штатное расписание → Графики смен → Оборудование) построены, см. «Что построено».
- **Доступы и видимость**: `managerOf`/`subordinateIdsOf` как единственный вход; `card.full_viewer` получает точку записи; «руководитель видит трудовую подчинённых»; `DocType.visibility = 'vertical'`; задачи подчинённых (`tasks.service.ts`); конструктор профилей доступа.

Прочие (порядок не зафиксирован):

- **Трекер привычек (B2C)** — первый арендатор «календаря-платформы» (розетка слоёв готова; модель Loop Habit Tracker — пропуск НЕ обнуляет стрик; ниша пуста во всех изученных OSS-календарях).
- **Малые проекты** (нужны все 4): ~~Заметки~~ (построены, [notes.md](notes.md)) → Импорт Kaspi-выписки → ~~Push~~ (web push построен как канал `core/notifications`; Expo/FCM — на mobile-этапе) → Счета.
- **Jobs Marketplace** (роли `mystery_shopper`/`contractor` зарезервированы) · **Отзывы (B2B)** (ресёрч готов — Serena `project_reviews_service`; Kaspi Merchant API проще всех) · **Додзё** (статус назначения training→certified заложен) · **Лента новостей (B2B)** (оси таргетинга уже в core/access) · **Процессы Ф5 RAG** (pgvector, отложен решением пользователя) · **Канбан-«Доски»** поверх Процессов (two-tier).
- **Движок QR-кодов** — заявлен пользователем как «очень важная штука»; место в очереди не определено.
- **Развитие «Объектов»** (структура готова, добавляется отдельными таблицами): акт передачи оборудования с подписью (`core/sign`), QR-инвентаризация, планы ППР и счётчики наработки, обмен сменами между сотрудниками, ставка `revenue_share` (сегодня только хранится), сроки КЭДО по РЕАЛЬНОМУ графику человека вместо жёсткого 5/2.

## Технический долг

**Границы движков (аудит 2026-09-02)**: правило «движки `core/*` не импортируют фичи» нарушено в коде в одном месте: `core/users` зовёт `modules/contacts` и `modules/workspaces` (активация приглашений по номеру) — решение НЕ выбрано, вопрос открыт (варианты — в gap-анализе, «Архитектура (долги)»). Долги «семь движков → `modules/notifications`» и «`core/rich-cards` → `modules/messenger`» закрыты движками **`core/notifications`** (17-й) и **`core/realtime`** (18-й): чат — канал доставки уведомлений, драйвер регистрирует мессенджер ([notifications_engine.md](notifications_engine.md), [realtime_engine.md](realtime_engine.md)). Остаток записан исключением в [module_graph.md](module_graph.md), §4, и в `KNOWN_CORE_TO_MODULES` стража `scripts/check-docs.cjs`.

**Уведомления — остаток после движка**: канал `email` — контракт без доставки (нет SMTP и верификации почты; в UI не показывается) · живые драйверы push `expo`/`fcm` — на mobile-этапе (модель `NotificationDevice` и контракт `PushDriver` готовы) · `follow`-подписка на объект есть в модели, UI — с первым потребителем · сводки/дайджесты по расписанию (Novu digest) — по запросу.

**Оргструктура (2026-09)**: кампании ознакомления молча обрезают аудиторию на `campaignMaxTargets` (движок адресатов сохранил семантику вызывающего; честный отказ — только при создании); `STAFF_FULL_SCOPE_ROLES` = `['owner','admin','manager']` — Менеджер пока полновластен над структурой, сужение до owner/admin одной константой вместе с ролью «Кадры»; матричное подчинение, проектные команды, онбординг-шаблоны структур — по запросу; переброс уже созданных заявок на заместителя не делается намеренно (снимки адресатов неприкосновенны).

**Мелочи аудита 2026-09-02**: `class-validator` в зависимостях API не используется (глобальный `ValidationPipe` инертен при Zod-входе) — снять или оставить как страховку; `API_URL` (Процессы) и `API_PUBLIC_URL` — две переменные одного смысла, слить в одну; запрет прямого импорта `@phosphor-icons/react` вне `Icon.tsx` не покрыт ESLint; у `PersonAvatar` своя шкала размеров (`sm|md|lg`), у `PersonChip` — `CardSize` XS–XL — при желании единой шкалы это правка кода.

**Прод-минимум (частично сделан)**: сделаны /api/v1, AllExceptionsFilter, helmet, fail-closed env, enableShutdownHooks. Остаётся: `/health`, Dockerfile API, verification-токен Google-вебхука, прод-наблюдаемость dead-letter джобов (`job.discarded` без подписчиков, /jobs/stats только dev), TURN/TLS 443 + wss для звонков, RNNoise-WASM.

**Mobile-подготовка (блок 8)**: push-пайплайн — модель `NotificationDevice` (platform ios/android, provider expo/fcm), ручки регистрации и контракт `PushDriver` ГОТОВЫ в `core/notifications`; остаётся живой Expo/FCM-драйвер и `expo-notifications` в приложении · пакет `design-tokens` (будущий, в `packages/`; палитра сейчас живёт только в CSS веба) + платформо-нейтральные поля CardSkinTokens · upload-модуль mobile · мобильные роуты зеркалят веб-пути (actionUrl = deep link). После подготовки mobile ≈ 50–55% новой работы.

**Кабинет платформы — решения ревью (2026-09-12, применены)**: состав штата (`platform.staff.*`) и сам тумблер four-eyes (`platform.policy.set`) идут через второго сотрудника (`dualControl` + `dualControlSoft` — пока владелец один, исполняются напрямую, иначе кабинет запирается насмерть) · `platform.pii.reveal` требует SMS-подтверждения (`stepUp`) · мёртвое право `processes.system.manage` снято (`tier: 'system'` не объявлен ни у одной ноды; когда появятся системные ноды, их откроет команда кабинета) · способности кабинета читаются только внутри `core/platform`, продуктовый путь их не спрашивает (страж `no-restricted-imports`, исключение — снятие со штата при анонимизации в `core/users`). Остаётся решить по ходу: узкие роли (`support`, `billing`, `security`) — сейчас живая роль одна, `platform_owner`.

**Кабинет платформы и тарифы — отложено осознанно (2026-09-12)**: токен кабинета живёт в `localStorage` — переезд на отдельный домен `admin.<домен>` с httpOnly-cookie, CSP и IP-allowlist · WebAuthn/passkeys вторым фактором вместо SMS · роль БД без UPDATE/DELETE на журналы (сегодня неизменяемость держит триггер) · стриминг журнала во внешнее хранилище · `view_as` (смотреть глазами человека) · кастомные роли поверх `PLATFORM_ROLES` · партиционирование `platform_audit_entries` по месяцам (поля уже все) · палитра ⌘K поверх реестра команд (когда команд станет 20+), сохранённые фильтры и очереди, экспорт журнала · оплата (`source: 'payment'`) и семейный план в `core/entitlements` · перенос прочих `isSystemAdmin`-операций в команды кабинета — [platform_console.md](platform_console.md), [entitlements_engine.md](entitlements_engine.md).

**Web-гигиена (остатки)**: `shop/page.tsx` на старом useState-стиле (70 useState — переводить при следующей работе над магазином) · распил >800-строчных файлов по ходу · правило кита для голых `<input>` линтером не покрыто.

**Известные ограничения (осознанные)**: юнит-тестов 0 (страж — e2e-сьют) · JWT_SECRET мастер-ключ без ротации (движок ключей) · Google-токены в БД открытым текстом (тот же движок ключей) · Google Calendar live OAuth не тестирован (нужны креды) · пер-экземплярные исключения повторов синкаются на уровне master+EXDATE.

## Внешние блокеры (действия пользователя, продукт не блокируют)

- Заявка на сервис Smart Bridge (NITEC-S-5096) — до одобрения QR-подписание в mock.
- Заявка на SDK KalkanCrypt (infra/sign-verifier: сборка → тест-контур test.pki.gov.kz → боевые корни).
- Заявка в ЦРТР на API ЕСУТД (до неё — mock + «отметить сданным вручную»).
- Договор Kazinfoteh (SMS) + альфа-имя у операторов.
- Юрист по формулировкам кадровых бланков.
- Прод-хостинг с БД в РК (ст. 12 ЗоПД).

## Решения «НЕ делать» (зафиксированы, не возвращаться)

- Микросервисы не нужны (синхронные деньги в одной транзакции — правильный монолит).
- core/access не переписывать (модель Zanzibar верна).
- SSR на вебе не нужен (приложение за логином).
- «Контакты» как слой отвергнуты — всё «Окружение» с Группами.
- relationshipType-категории и «системная папка Незнакомец» — отвергнуты.
- MVP-подход запрещён (см. CLAUDE.md, «Философия разработки»).
