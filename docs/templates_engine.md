# core/templates — заполнение шаблонов документов

> Тонкая подсистема БЕЗ своих таблиц: синтаксис + компилятор + форматтеры — НАШИ и вечные; **драйвер рендера — СВОЙ и сменный** (docx + builder). Владелец данных отдаёт группу полей регистрацией; заполнение — сервисными методами.

## Синтаксис (формат-агностичный, от драйвера не зависит)

`{Организация.БИН}` · форматтеры цепочкой `{Поле|дата:долгая}` · повтор строки таблицы `{#Строки}…{/Строки}` + `{№}`. Шаблоны клиентов написаны на этом синтаксисе — драйвер и синтаксис не трогать (упрёмся — подключается запасной драйвер без переделки шаблонов).

## Драйвер docx (`docx-render.driver.ts`, fflate)

Текст-узлы `<w:t>` собираются в «виртуальный текст» с картой смещений — тег, РАЗОРВАННЫЙ Word'ом на прогоны, находится и собирается; между абзацами/`<w:br/>` — сентинел (через него тег честно НЕ собирается, `tag_broken_by_break`); замена — хирургия только внутри текст-узлов (значение наследует форматирование первого узла); многострочные значения — настоящие `<w:br/>`; повтор = клонирование `<w:tr>`.

**Ошибки только громкие**: `TemplateCompileError` (битые теги, парность — ПОЛНЫМ списком) и `TemplateDataError` (список недостающих полей). Слово «undefined» в документе невозможно by construction.

## Драйвер builder (`builder-render.driver.ts` + `PdfRenderService`)

Блочные шаблоны конструктора (формат блоков — СВОЙ в shared: `types/doc-builder.ts` + рекурсивный Zod): блоки → печатный HTML А4 (PT Serif — казахский алфавит; шрифты `infra/pdf-fonts/` замонтированы в контейнер И подключены в вебе — превью и PDF одним шрифтом; ГОСТ-подобные поля; missing по контракту честности: strict → `TemplateDataError`, мягко → видимая метка ‹…›) → **Gotenberg** (профиль pdf, :3030; печатает Chromium — совпадение с браузерным превью 1:1; колонтитул «стр. N из M» — нативный footer, НЕ preferCssPageSize). `GOTENBERG_URL` пусто → рендер выключен честно.

**Чип данных в конструкторе атомарен** (`path` в формате docx-тегов «Организация.БИН») — реестр полей и форматтеры ОБЩИЕ для обоих видов шаблонов; сломать тег изнутри невозможно. Публикация builder-шаблона валидируется `checkBuilderDoc`: чипы сверяются против реестра полей И объявленной формы, отказ — 400 ПОЛНЫМ списком; «пустой шаблон» = нет ни одного блока С СОДЕРЖИМЫМ, а не `blocks.length === 0`.

## Форматтеры (`template-formatters.ts`)

`дата`/`дата:долгая` (строка «2026-09-01» разбирается КАЛЕНДАРНО, без часовых поясов; моменты — через APP_TIMEZONE) · `прописью` (тенге/тиын до триллионов) / `прописью:число` · `число` (разряды пробелами, запятая-дробь).

## Реестр групп полей

```ts
TemplateFieldRegistry.register({ key, tagPrefix, label, fields, resolve(ctx) })  // onModuleInit владельца данных
```
Группы: «Организация» (Workspaces: реквизиты + основной счёт + директор; тумблеры видимости реквизитов здесь НЕ действуют осознанно — право формирования проверяет потребитель) · «Сотрудник» (Staff: анкета + должность/отдел/объект + **`Руководитель`, `Руководитель Должность`, `Руководитель объекта`, `Руководитель объекта Должность`** — по факту через `managerOf`/держателей головы объекта, вершина → владелец; ФИО = Фамилия Имя Отчество; приоритет должности — legalPositionId из трудовой карточки, затем основное место) · «Документ» · «Контрагент» · «Договор»/«Действие»/«Подписант» (КЭДО). Контекст резолва несёт `templateId`/`hrActionId`.

**Контракт честности резолвера**: незаполненный реквизит = null → рендер откажет СПИСКОМ («заполните Анкету»); осознанно-пустое (не-плательщик НДС) = ''.

## Компилятор (`template-compiler.ts`)

Опечатки против реестра, неизвестные форматтеры/аргументы, `{№}` вне повтора; форма элемента повтора — через `extraPaths`. Периоды дат: kind поля `daterange` разворачивается в плоские ключи «X С»/«X По»/«X Дней»/«X» (`expandDocFormValues` в shared) — глубоких путей в синтаксисе НЕТ намеренно.

## Сервисный API

`TemplateRenderService.render(buffer, values)` / `compile(buffer, {extraPaths})` / `renderForContext(buffer, ctx, extraValues)` / `systemRenderFileForContext(fileId, ctx, …)` — **system-контракт: право формировать проверяет вызывающий**. PDF из заполненного .docx — ноль кода (rendition-путь core/docs).

`GET /templates/field-groups` — панель «Что подставить» конструктора. Дев-полигон `POST /templates/dev/render|compile|resolve` (только development).

## Почему свой драйвер (зафиксировано проверкой на живых файлах)

easy-template-x МОЛЧА ломает документ · docxtemplater: Excel — платный модуль, `undefined` без nullGetter · carbone: дата уезжает на день без timezone, закрытая лицензия v4+. Excel — вторым драйвером при Счетах (перенумеровка строк по образцу MIT xlsx-template).

## Проверка

`verify-templates.cjs` (включая смоук «живая Collabora конвертирует файл нашего драйвера в PDF»), `verify-doc-builder.cjs` (с живым Gotenberg).
