/*
 * Обезличивание редактора — ШТАТНАЯ точка расширения, а не хак.
 *
 * cool.html подключает /browser/<hash>/branding.js всегда; в оригинальной поставке
 * этот файл генерирует приватный brand.sh Collabora (недоступный нам), а в сборке
 * из исходников его просто НЕТ — запрос отдаёт 404. Из-за этого переменная
 * brandProductName остаётся неопределённой, и клиент подставляет своё запасное имя
 * «Collabora Online Development Edition (unbranded)» — оно и попадает в диалог
 * «О программе» и в сообщения о подключении.
 *
 * Настройки сервера (user_interface.brandProductName) до браузера НЕ доезжают:
 * клиент читает именно эти глобальные переменные. Поэтому кладём свой файл.
 *
 * Что читает клиент (сверено по bundle.js и global.js на нашей версии):
 *   brandProductName    — имя продукта в интерфейсе и в диалоге «О программе»;
 *   brandProductURL     — ссылка на продукт;
 *   brandProductFAQURL  — ссылка на справку.
 *
 * Пустые ссылки означают «ссылки нет» — это правильнее, чем вести пользователя
 * на чужой сайт из нашего продукта.
 *
 * ВАЖНО про атрибуцию: этот файл убирает только ТОВАРНЫЙ ЗНАК из интерфейса, как
 * того требует политика Collabora для изменённых сборок. Уведомления об авторских
 * правах и лицензии (в cool.html, COPYING, licenses/) не трогаются — MPL-2.0 §3.4
 * прямо это запрещает.
 */
var brandProductName = 'SuperApp6';
var brandProductURL = '';
var brandProductFAQURL = '';
