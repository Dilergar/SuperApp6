# SuperApp6 — CLAUDE.md

**SuperApp6** — экосистема **B2C + B2B**: «ERP для жизни и бизнеса». Один аккаунт, один `user_id` на всё — и внутри 100+ сервисов (целевой масштаб): задачи, календарь, чат, финансы, магазин, документы, кадры, звонки… Рынок — Казахстан; идея на стыке суперапп-подхода WeChat/Kaspi и ERP (Salesforce/Odoo). B2B живёт в том же приложении как организации-арендаторы (заголовок `X-Workspace-Id`).

**Карта документов проекта:**
- `CLAUDE.md` (этот файл) — правила работы и несущие принципы;
- **`docs/`** — КАНОН архитектуры: движки, сервисы, конвенции, ловушки (индекс — `docs/README.md`);
- `PRODUCT.md` — продукт, пользователи, позиционирование;
- `DESIGN.md` — дизайн-система (ОБЯЗАТЕЛЕН перед любым UI);
- `docs/gap_analysis_v2.md` — живой план; `docs/roadmap.md` — этапы и техдолг.
- `docs/i18n.md` — мультиязычность (ОБЯЗАТЕЛЕН перед любым текстом для человека).

---

## 1. Think Before Coding

Don't assume. Don't hide confusion. Surface tradeoffs.
Before implementing:

- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Философия разработки: Никакого MVP

- **Правило долговечности:** При работе над новыми частями и функциями проекта термины и подходы класса «MVP» (минимально рабочий продукт) строго запрещены. У нас нет задачи «проверить гипотезы» и выкатить неустойчивую заплатку.
- **Качество по умолчанию:** Мы проектируем и пишем код правильно, надолго и архитектурно устойчиво. Наши компоненты и модули должны быть изначально готовы к горизонтальному масштабированию, отвечать строгим стандартам безопасности, производительности, консистентности данных и изоляции (B2B/B2C).
- **Проектирование наперёд:** Во время планирования и реализации мы не обрезаем функционал, а думаем на несколько шагов вперёд (на уровень AI-инструментов, будущей интеграции с IoT, мобильным клиентом и т.д.). Пользователь (разработчик) сам укажет рамки реализуемой версии, если это потребуется.
- Перед крупной стройкой — **грилл дизайна**: trade-offs + бенчмарки гигантов, спорное — вопросами пользователю (с рекомендациями), второстепенное — автодефолт решениями гигантов.

## Язык-источник — английский
**Source locale = `en`. Target locales = `kk`, `ru`.** Называй по-английски всё, что
живёт в коде: ключи каталога, поля реестров, коды отказов, имена в схеме.

- Текст для ЧЕЛОВЕКА — ключ каталога `@superapp/i18n` (`useTranslations` /
  `I18nService`). Английская фраза этого ключа пишется в `en`-каталог, а НЕ в код.
- Текст для РАЗРАБОТЧИКА (Swagger, логи, `new Error`, env-валидация) — английский
  литерал прямо в коде.
- Комментарии — русские.
- Язык формата — переменная зрителя: даты, числа, деньги, размеры только через
  форматтеры платформы. Литерал локали не писать никогда: ни `'ru-RU'`, ни `'en-US'`.
- Каждый новый ключ заводить сразу в три каталога: `en`, `kk`, `ru`.
- Детали и ловушки — `docs/i18n.md`.

Cybersecurity - очень важен для такой экосистемы
При создании новых сервисов, фукнции, фич - не забывай про Cybersecurity и планируй её сразу с высоким уровнем безопастности 
**Увидел уязвимость, дыру — чинишь сразу, где бы она ни была.** В том же подходе, без отдельного разрешения: правка на ВСЕХ путях


## 3. Главный инструмент поиска: graphify

В проекте развёрнут локальный индексатор и анализатор кодовой базы на основе графов — **graphify**. Этот инструмент имеет приоритет над brute-force чтением и поиском.

- **Правило первого хода:** ИИ-агент ОБЯЗАН использовать локальные команды утилиты `graphify` перед выполнением широкого поиска по коду с помощью `rg` (ripgrep), glob-поиска или построчного чтения больших файлов.
- Команды: `graphify query "<вопрос>"` (скоупленный подграф) · `graphify path "<A>" "<B>"` (связь между сущностями) · `graphify explain "<концепт>"`. Результат обычно в разы меньше сырого grep.
- После изменения кода И после правок доков: `graphify update .` из CLI (AST-only, без API-затрат, без субагентов) — граф остаётся актуальным. Полное семантическое обновление графа доков через скилл `/graphify` (субагенты-экстракторы) стоит миллионы токенов — только ОДИН раз в конце крупной задачи и только по явной просьбе пользователя.
- Правило принудительно: в `.claude/settings.json` стоят hook-guard'ы на Bash/Grep/Read/Glob.
- `/graphify` — триггер скилла `.claude/skills/graphify/SKILL.md` (любой ввод → граф знаний).
- Точечное чтение/правка конкретных строк и файлов, которые graphify уже указал, — нормальный следующий шаг (для навигации по символам есть Serena, §12).

## 4. Документация docs/ — читать до, дополнять после

**Перед началом работы изучай содержимое папки `docs/`** (индекс `docs/README.md` + файлы, релевантные задаче) и следуй правилам проекта. **Если увидишь несоответствия** (док ↔ код, док ↔ док) — **сообщай об этом и предлагай исправление и доработки.** Если всё очевидно и понятно — **в конце каждого подхода к работе дополняй документацию в папке `docs/`** (актуализируй существующую, создавай новую по образцу соседних) **и прогоняй `pnpm check:docs`** — он ловит мёртвые пути, пропуски в индексе, env вне схемы и новые рёбра между модулями.

Стиль доков: только текущее состояние + вечные правила и ловушки, без истории изменений (история — в git; исключение — `docs/roadmap.md`).

## 5. Источники правды и память

| Уровень | Что хранит | Правило |
|---|---|---|
| `docs/` | **Канон архитектуры** | Изменил архитектуру/API/модель → обнови соответствующий док |
| `CLAUDE.md` | Несущие правила | Обновляется, только если меняется правило уровня платформы |
| Serena memories (`.serena/memories/`) | Дизайн-исследования, история решений | Новые архитектурные факты сюда НЕ дублировать — они в docs/ |
| Auto-memory Claude | Личные заметки агента (уроки, предпочтения) | Не подменяет docs/ |

Прочее: `graphify-out/` — артефакты графа (генерируются); `acc.txt`, `*-dev.log` — локальные, не в git.

---

## 6. Что уже работает

**28 платформенных движков** (`apps/api/src/core/`): access (ReBAC) · rich-cards · search · quick-actions · files · voice (STT) · calls (LiveKit) · chatter · jobs (outbox) · verify (SMS-OTP) · docs (WOPI) · share-links · approvals · sign (ЭЦП+ПЭП) · templates · audiences (адресаты) · notifications (уведомления: in-app/push/SMS/chat, предпочтения, политика организации) · realtime (один сокет `/realtime`) · entitlements (тарифы и лимиты: планы с версиями, триал/грейс, гранты, оверрайды, квоты, `402 entitlement.*`) · platform (Кабинет платформы: сотрудники ≠ user_roles, команды с журналом/идемпотентностью/step-up/«четыре глаза», карточка 360) · analytics (продуктовая аналитика: реестр событий в shared, приём без БД на пути запроса, партиции PostgreSQL, роллапы, язык запросов, дашборды и конструктор отчётов в Кабинете, SDK `@superapp/analytics`) · keys (движок ключей: корень/keystore, envelope с AAD, слепые индексы, Ed25519-подпись + JWKS, ротации, шифрование ПДн, ключи API и боты, реестр/журнал, step-up) · webhooks (исходящие вебхуки: реестр событий в shared, `emit(tx)`, Standard Webhooks, ретраи, аудит подписи) · consents (движок согласий: документы платформы версиями в БД с подписью, приёмка-доказательство в транзакции действия, шлюз новой версии, учёт действий с ПДн, журнал инцидентов, «Мои данные», `/legal`) · idempotency (идемпотентность повторов: ключ `Idempotency-Key` на всех мутациях по умолчанию, отметка «эффект закоммичен» в бизнес-транзакции обёрткой `$transaction`, снимок ответа под KEK владельца на 72 ч, «входящий ящик» ровно-одного-раза для чужих событий, производные ключи вниз по стеку, страж объявлений на буте) · audit (журнал аудита безопасности: единый append-only поток `security_events` с тремя зрителями — человек / организация / платформа, реестр событий в shared, запись в транзакции факта, триггеры ENABLE ALWAYS + роль-владелец, защита входа по аккаунту, сессии-семейства, cooling, экстренная заморозка и «Это не я», журнал организации по тарифу + экспорт + SIEM-стрим, дайджесты Меркла, архив, детекции) · visibility (правила видимости ПОЛЕЙ: реестр типов записей в shared, личное поле решает человек, служебное — организация матрицей «поле × адресат», провод `Guarded<T>` = значение | маска | скрыто, маски только на сервере, потолки класса поля и бота/ключа, раскрытие одной записи под SMS с записью в журнал, «четыре глаза» на ослабление, находимость по номеру, хроника маскируется при чтении, страж ответа + канареечный сьют) · lifecycle (жизненный цикл и архитектура данных: реестр политик ВСЕХ хранилищ — модели, сырые таблицы, байты, семейства Redis, архивы — с классом, владельцем, сроком «пол закона / умолчание / потолок», стиранием субъекта и удалением организации; нативный uuid v7; партиции журналов под функциями владельца; раннер сроков, каскад организации, loose FK; заморозки на всех путях удаления; стирание с квитанцией, сертификатом и канарейкой; сроки организаций и таймеры чатов; выгрузки целиком и восстановление одной организации; PgBouncer, потолки ролей, Redis «состояние» + «кэш», бэкапы с учениями, `/health` и сторожевые метрики).

**Сервисы** (`apps/api/src/modules/`): Окружение (Circle — фундамент) · Задачник · Календарь (+Google) · Мессенджер · My Wish & Shop · Кошелёк-леджер · Скины карточек · Организации · Сотрудники + Орг. структура (вертикаль на графе должностей и объектов) · Процессы (нодовый канвас) · Финансы (B2C) · Диктофон · Виртуальный офис · Диск (OmniDrive) · Документооборот (+ЭДО) · Контрагенты · КЭДО (HR) · Объекты (дерево площадок + юрлица + штатное расписание + график смен + оборудование) · Заметки (B2C+B2B: свой формат документа, доска-вид на раздел с Alt+N на любой странице, привязка к задачам/контрагентам/объектам/документам). Документная вертикаль ЗАВЕРШЕНА; B2B-вертикаль объектов ПОСТРОЕНА; **мультиязычный фундамент построен** (kk/ru/en, render-at-read, стражи — `docs/i18n.md`), сервисы переводятся по одному за сессию.

Все сервисы — «между людьми»: Мама ставит задачу Сыну, семья скидывается на подарок, сотрудник пишет коллеге по «рабочему пропуску».

Этапы дорожной карты: **1) сейчас — новые сервисы** → 2) Mobile (переписать) → 3) AI (ассистенты + SuperAIAgent6) → 4) Терминал + IoT. Детали — `docs/roadmap.md`.

## 7. Принципы платформы (обязательны для КАЖДОГО нового сервиса)

1. **Сначала переиспользуй, потом пиши.** Новый сервис = тонкий модуль + регистрации в движках. Движка не хватает → он РАСШИРЯЕТСЯ, а не копируется. Карта готового — ниже.
2. **Человек в UI = карточка, ВЕЗДЕ.** Только `PersonChip`/`PersonAvatar` (5 размеров XS–XL), не голый текст — несущее продуктовое правило (видимость платных скинов). **Защищённое поле = `<GuardedValue>`** (значение / маска / «Скрыто» решает сервер, `core/visibility`); раскрытие — только в карточке одной записи. **Бот — `BotChip`/`BotAvatar`** (`components/keys/`), никогда PersonChip: актор с `kind: 'bot'` в хронике и уведомлениях рисуется ботом.
3. **Rich Card — где уместно и было бы отлично добавить для удобства.** Сущность со статусами и действиями обязана уметь рендериться рич-картой (`core/rich-cards`).
4. **AI-ready.** Каждая операция вызываема программно: тонкий контроллер + Zod + способность (= будущие AI-инструменты). Чтение AI — напрямую из БД строго через `core/access`; действия — только через сервисные API (денежное — без исключений).
5. **Device-agnostic.** Веб, mobile (этап 2), Терминал — равноправные клиенты ОДНОГО API `/api/v1`. Пока нативного приложения нет, мобильный веб обязан работать.

**Карта «нужно сервису → готовый движок»** (детали и контракты — в указанных доках):

| Нужно | Движок / механизм | Док |
|---|---|---|
| Права, шеринг, роли | `core/access`: тип в схеме + проекция + способности | `docs/access_engine.md` |
| Массовая выборка под правами | `grantSetFor` + свой SQL (НЕ check() в цикле, НЕ listObjects) | `docs/access_engine.md` |
| Кто видит КАКИЕ ПОЛЯ записи и в каком виде (ИИН, оклад, телефон, ставка…) | `core/visibility`: тип в реестре shared (`packages/shared/src/visibility/<сервис>.ts` + `visibility.types.*` в трёх каталогах) + `VisibilityTypeRegistry.register` в модуле-владельце + `shape()` пачкой на КАЖДОМ пути чтения, `assertWritable` на правке, `assertQueryable` на фильтре/сортировке + строка `CANARY_TYPES`; свои маски и маркер `$v` запрещены линтером | `docs/visibility_engine.md` |
| Интерактивная карточка | `core/rich-cards`: реестр рендер+действия | `docs/rich_cards.md` |
| Глобальный поиск | `core/search`: +1 провайдер | `docs/search_engine.md` |
| Кнопка в меню чата | `core/quick-actions`: +1 регистрация | `docs/quick_actions.md` |
| Файлы, вложения, фото | `core/files`: профиль + FilesRefRegistry + linkFile | `docs/files_engine.md` |
| Офисный документ (совместная правка) | `core/docs` (WOPI): from-file + canEditContent | `docs/docs_engine.md` |
| Голос, расшифровка | `core/voice`: requestTranscript / transcribeSync | `docs/voice_engine.md` |
| Звонки, конференции | `core/calls`: CallsRefRegistry + POST /calls/token | `docs/calls_engine.md` |
| Хроника «было → стало» | `core/chatter`: log(tx) СИНХРОННО в транзакции мутации | `docs/chatter_engine.md` |
| Фоновая работа (обязана случиться) | `core/jobs`: enqueue(tx) + идемпотентный обработчик | `docs/jobs_engine.md` |
| Сайд-эффекты между модулями | EventBus — ТОЛЬКО то, что можно потерять | `docs/event_bus.md` |
| SMS-код (владение номером, step-up) | `core/verify`: purpose + consume(tx) | `docs/verify_engine.md` |
| Поделиться наружу (без аккаунта) | `core/share-links`: реестр + гостевая страница /s/ | `docs/share_links_engine.md` |
| Собрать решение (согласовать/подписать/ознакомиться) | `core/approvals`: describeForCreate проверяет право | `docs/approvals_engine.md` |
| Юридическая подпись (ЭЦП/ПЭП) | `core/sign`: движок сам замораживает предмет | `docs/sign_engine.md` |
| Данные в шаблон документа | `core/templates`: TemplateFieldRegistry + renderForContext | `docs/templates_engine.md` |
| **Текст для человека (любой)** | `@superapp/i18n`: ключ каталога + `useTranslations`/`I18nService`; литерал запрещён линтером | `docs/i18n.md` |
| Уведомления человеку | `core/notifications`: тип в реестре shared (`packages/shared/src/notifications/<сервис>.ts` + `.title/.label` в трёх каталогах) + `send(tx, …)` В транзакции мутации + `NotificationRefRegistry` (право видеть + deep link); продюсер решает КОМУ, движок — КАК (каналы, предпочтения, тишина, схлопывание) | `docs/notifications_engine.md` |
| Событие в сокет / команда с клиента | `core/realtime`: `registerRelay` / `registerHandler` / `registerConnectionHook`; свой gateway запрещён | `docs/realtime_engine.md` |
| Платная фича, потолок «сколько можно», расходуемая квота | `core/entitlements`: ключ в реестре shared + `assertFeature`/`assertCanCreate(tx)`/`consume(tx)` в сервисе + замок/шкала в вебе; потолки в константах и 403 «за тариф» запрещены — только `402 entitlement.*` с `unlock` | `docs/entitlements_engine.md` |
| Поведение человека / продуктовая метрика (воронка, активность, adoption) | `core/analytics`: событие в реестре shared (`packages/shared/src/analytics/<область>.ts` + `analytics.events.<ключ>.title/description` в трёх каталогах) + `track(tx, …)` В транзакции мутации (факт сервера) или `analytics.track` SDK (клиент: «увидел/попытался»); свойства — только коды и признаки, никогда текст и PII | `docs/analytics_engine.md` |
| Секрет, ключ, шифрование, подпись, HMAC, ключ API, бот | `core/keys` — ЕДИНСТВЕННАЯ дверь: `KeysEnvelopeService` (+`KeysFieldRegistry` для колонки), `KeysSigningService` (аудитория), `KeysMacService`; линтер запрещает вне движка `JWT_SECRET`, библиотеки JWT (`jsonwebtoken`/`jose`/`@nestjs/jwt`) и примитивы шифрования `node:crypto` (cipher/keypair/kdf; хеши, HMAC, random остаются), а заголовки `authorization`/`cookie` в логах — везде; ПДн — прозрачно через Prisma-расширение (`PII_MODELS`); ключи API открываются сервису строкой в `KEY_SCOPE_SERVICES` | `docs/keys_engine.md`, `docs/keys_api_access.md`, `docs/keys_pii.md` |
| Согласие человека или организации с платформой; передача ПДн третьему лицу; новый юридический документ | `core/consents`: вид в реестре shared (`packages/shared/src/consents/registry.ts`) + `consents.accept(tx, …)` В транзакции действия (приёмка вне транзакции отвергается) + `afterCommit()`; внешний получатель данных — строка в `recipients.ts` + `actions.record(tx, …)` в месте фактической передачи + новая версия `cross_border`; данные, живущие на согласии, гасит хук `ConsentsRevokeRegistry`; юридический ТЕКСТ — контент в БД, в каталог i18n не кладётся никогда; новый маршрут, нужный за блокирующим экраном, — `@SkipConsentGate()` | `docs/consents_engine.md`, `docs/legal_kz.md` |
| Событие безопасности: вход, сессия, права, ключи, выгрузка, доступ к ПДн, действие сотрудника платформы | `core/audit`: ключ в реестре shared (`packages/shared/src/audit/<область>.ts`: категория, серьёзность, три зрителя, строгие детали — только коды) + `audit.events.<key>.title|body` в трёх каталогах + `audit.record(tx, …)` В транзакции факта; уведомление человеку — полем `notify` реестра (не дублировать); выгрузка данных — `data.export{source}`; свои журналы действий не заводить | `docs/audit_engine.md` |
| Повтор запроса не должен удвоить эффект (любая мутация) | `core/idempotency` — по умолчанию УЖЕ покрыто; объявить `@Idempotent({required})` на необратимом, `@SkipIdempotency(причина из закрытого списка)` на исключении; ключ вниз по стеку — `deriveKey(hop)`/`stableKey(ns,…)`; чужое событие без нашего ключа — `IdempotencyInboxService.once(tx, …)` ПОСЛЕ проверки подписи | `docs/idempotency_engine.md` |
| Событие наружу (интеграция клиента, 1С, CRM) | `core/webhooks`: событие в реестре shared (`packages/shared/src/keys/webhook-events.ts` + `keys.webhook.events.<key>` в трёх каталогах) + `webhooks.emit(tx, …)` В транзакции мутации; свой `fetch` наружу запрещён | `docs/webhooks_engine.md` |
| Операция для сотрудника платформы (поддержка, биллинг, безопасность) | `core/platform`: команда в `PlatformCommandRegistry` / панель в `PlatformPanelRegistry` (поиск — `PlatformLookupRegistry`) из своего модуля; свои контроллеры под `/platform` не заводить | `docs/platform_console.md` |
| Деньги: оплата, заморозка, сделки | `wallet` (Ledger + Escrow) — только синхронно в одной tx | `docs/wallet_ledger.md` |
| Записи на сетке календаря | Реестр слоёв (регистрирует ВЛАДЕЛЕЦ данных) | `docs/calendar.md` |
| «Человек достижим?» | `ContactsService.assertReachable` (+personalOnly для личного) | `docs/contacts_circles.md` |
| Состав Группы | ТОЛЬКО `resolveCircleMemberIds` | `docs/contacts_circles.md` |
| Кому адресовано (отдел/должность/объект/Группа/руководитель кого-то) | `core/audiences`: `resolve(refs, ctx, opts)` + свой набор видов; движок решает, гранты не пишет | `docs/audiences_engine.md` |
| «Кто мой руководитель / моя команда» | адресаты `manager_of`/`subordinates_of`/`branch_head_of` (внутри Staff — `managerOf`); свой обход структуры запрещён | `docs/org_structure.md` |
| Отзыв при разрыве связи | `PersonalGraphRegistry.register(...)` — обязателен при грантах из окружения | `docs/contacts_circles.md` |
| Файлы сущности → на Диск | `DriveRoutingRegistry.register(...)` | `docs/drive.md` |
| Новая таблица, колонка ПДн, файл, ключ Redis — сколько живёт, кто владелец, что при стирании человека и удалении организации | `core/lifecycle`: политика в реестре shared (`packages/shared/src/lifecycle/<область>.ts`: класс, владелец, основание, срок, `onSubjectErasure`, `onTenantPurge`, `exportable`) + шаг purge/хук/шаг стирания своим `<модуль>.lifecycle.provider.ts`; своё удаление по сроку (`deleteMany` в кроне) запрещено; незарегистрированное роняет `check:lifecycle` и бут; кэш Redis — только `RedisService.cache` | `docs/lifecycle_engine.md`, `docs/data_architecture.md` |
| Данные организации без внешнего ключа на неё (полиморфный `ownerType = workspace`) | `onTenantPurge: registry_hook(key)` в реестре core/lifecycle + `LifecycleTenantHookRegistry.register(key, { purge })` в `<модуль>.lifecycle.provider.ts` — каскад окончательного удаления стирает их ТВОИМ путём; иначе переживут организацию навсегда | `docs/lifecycle_engine.md`, `docs/workspaces.md` |
| Закрыть папку от наследования | `DriveNode.restricted` | `docs/drive.md` |
| Вторая сторона договора (без аккаунта) | `modules/counterparties` — единый справочник | `docs/counterparties.md` |
| Исходящий HTTP наружу | Две двери `shared/http`: safeFetch (адрес из данных) / trustedFetch (из .env) | `docs/security.md` |
| Физическая площадка (точка, здание, этаж, склад) | `modules/objects` — дерево `StaffBranch` (`ancestorIds`+GIN), права через предков `capsFor` | `docs/objects.md` |
| Штат, ставки, смены, оборудование объекта | тот же модуль: `StaffingPosition`/`StaffRate` · `Shift`/`ShiftAttendance` · семья `Asset` | `docs/objects_staffing.md`, `docs/objects_shifts.md`, `docs/objects_assets.md` |
| Сторона договора и счёта (ТОО/ИП внутри организации) | `LegalEntity` (головное — самолечение; `/requisites` = головное) | `docs/legal_entities.md` |
| «Кто работает СЕЙЧАС» (назначения датированы) | `activeAssignmentWhere()` у КАЖДОГО потребителя + джоб `staff.assignment.rollover` | `docs/objects.md` |
| B2B-изоляция | chokepoint `X-Workspace-Id` ИЛИ ownerType+ownerId + права | `docs/identity_roles.md` |
| Выбор человека/сущности в UI | `EntitySelector` (контекстный тип user) | `docs/web_conventions.md` |
| Значок сущности | `GlyphField`/`Glyph`; из API — ключ реестра иконок | `docs/web_conventions.md` |

## 8. Архитектура (несущее)

- **Модульный монолит** NestJS: каждый сервис — изолированный модуль. Связи ДВУХ видов: синхронные вызовы (атомарность: деньги в одной `$transaction`) и EventBus (Redis Streams, **at-most-once** — только то, что можно потерять). Обязательный фон — `core/jobs` (transactional outbox). **Новое синхронное ребро → добавить в `docs/module_graph.md`.** Циклы — ленивые токены из манифеста `shared/di-tokens.ts` (смоук на bootstrap).
- **Universal Identity**: роли в `user_roles(user_id, role, context, tenant_id)`; лестница `contractor < trainee < staff < manager < admin < owner`; найм всегда в trainee; contractor изолирован. Движки `core/*` не импортируют фичи — обратное направление только через реестры.
- **Контракт API↔клиенты** (три эшелона — компилятор, линтер, правила): одна форма провода = один тип в `@superapp/shared` на ОБЕИХ сторонах; вход = `z.infer` из Zod-схемы; страница едет в `data` ЦЕЛЬНОЙ (`CursorPage`/`OffsetPage`); клиенты — только хелперы `@superapp/api-client`; локальный interface серверного DTO запрещён. Детали — `docs/contract_boundary.md`.
- **Безопасность**: fail-closed env (всё, кроме явных development/test, = прод); tokenEpoch отзывает access-токены; IP только из `req.ip` (TRUST_PROXY); голый `fetch` в API запрещён линтером. Детали — `docs/security.md`.
- Решения «НЕ делать» (не возвращаться): микросервисы; переписывание core/access; SSR веба; слой «Контакты» (всё — «Окружение»). Полный список — `docs/roadmap.md`.

## 9. Несущие правила (выжимки; полные версии — в доках)

**API** (`docs/api_conventions.md`):
- Флаг в query — ТОЛЬКО `queryBoolean` (никогда `z.coerce.boolean()`: строка `'false'` истинна).
- Статические пути объявлять ДО `:id` (иначе Nest ищет сущность «mine»/«inbox»).
- Методы `system*` движков прав НЕ проверяют — проверяет вызывающий (первое, что смотрит ревью).
- Переходы состояний — status-guarded `updateMany`; идемпотентность — уникальные индексы; партиальные уникумы — руками в миграцию + коммент в схеме.
- Время в сыром SQL — только `utcTs()`/`ts(d)`; `db push` запрещён (только prisma migrate).
- Все ошибки — единый конверт через `AllExceptionsFilter`: `details.code` есть ВСЕГДА, `message` сервер ПЕРЕВОДИТ в языке запроса. Новый отказ пишется без текста — `throw notFound('code')` / `forbidden` / `badRequest` (`shared/errors/api-error.ts`).

**Веб** (`docs/web_conventions.md`):
- Нативные `confirm()`/`alert()` ЗАПРЕЩЕНЫ → `useConfirm()` / `toastError()`.
- Примитивы — только кит `components/ui/`; человек — только `PersonChip`/`PersonAvatar`; защищённое поле — только `<GuardedValue>`; пикеры — `EntitySelector`; пикер не предлагает того, что сервер отвергнет.
- RQ-ключи — в `lib/queries.ts`; один ключ = ОДНА форма кэша (useQuery и useInfiniteQuery несовместимы на общем ключе).
- **Строка для человека — только ключ каталога** (`useTranslations`), никогда не литерал: строка в коде = один язык навсегда. Форматирование дат/чисел/денег — только `@superapp/i18n` (`toLocaleDateString('ru-RU')` запрещён).
- Новый сервис = +1 строка `lib/app-nav.ts` (`labelKey`) + 1 файл `<сервис>/loading.tsx` + 1 файл `<сервис>/layout.tsx` с `<ServiceMessages ns="…">` (в корне app/ — НЕЛЬЗЯ).
- Смысл несёт ФОРМА (статус = Chip, действие = Button); цвета не придумывать — нет в DESIGN.md → спросить (исключение — цвет-данные); UI несуществующих фич не показывать.

**Данные и фон** (`docs/jobs_engine.md`, `docs/module_graph.md`):
- Деньги и security-обязательные эффекты — только синхронно в одной транзакции.
- Джоб ставится `enqueue(tx, …)` В транзакции мутации; обработчик идемпотентен, делит ошибки на транзиентные (throw) и постоянные (JobDiscardError); клейм не коммитить до эффектов.
- Хроника пишется `log(tx, …)` синхронно в транзакции мутации.

**Процесс работы**:
- **Субагентов (инструмент Agent) НЕ вызывать вообще** — ни для поиска, ни для стройки, ни для graphify, — если пользователь сам явно не написал «используй субагентов». Субагенты работают параллельно и съедают квоту в разы быстрее (одна стройка канваса = 12 млн эффективных токенов). Если субагент всё же разрешён — правило graphify-first включать в его промпт.
- Контекст беречь: большие файлы читать фрагментами (`sed -n`/offset), не перечитывать доки целиком, не пересобирать проект после каждой мелкой правки — каждый ход отправляет весь накопленный контекст заново.
- Стража/правило проверять НА СРАБАТЫВАНИЕ (подсадить нарушение → увидеть падение → убрать).
- Отчёт о готовности этапа — сверив план ПОСТРОЧНО (опаснее всего «бэкенд есть, веб не зовёт»).
- Правило действует, только если стоит во ВСЕХ путях (оба пути удаления и т.п.).
- После UI-правок — браузерная проверка (0 ошибок консоли); ловушки проверки — `docs/platform_gotchas.md`.

## 10. Команды

> **Windows:** tsc НЕ работает из Git Bash — сборки через PowerShell (`powershell -Command "cd path; command"`). Полный tsc на apps/api падает по памяти — только `nest build`.

```bash
docker compose up -d                        # PostgreSQL 18 + Redis 8.10 ×2 (версии пинованы)
pnpm install
cd packages/shared && pnpm build            # ПОРЯДОК НЕСУЩИЙ: shared → i18n → api-client → api/web
cd ../i18n && pnpm build                    # prebuild сам пересоберёт messages/index.ts
cd ../api-client && pnpm build
cd apps/api && pnpm db:generate && npx prisma migrate deploy   # схему менял → pnpm db:migrate
pnpm dev                                    # всё сразу (API :3001, Web :3000, Swagger /api/docs в dev)
pnpm lint:guard                             # оба линт-стража (~7с, из корня)
pnpm check:docs                             # страж документации (~2с): пути, индекс, env, рёбра модулей; --write обновляет module_graph_edges.md
pnpm check:i18n                             # страж каталогов (~1с): паритет ключей en/kk/ru, разбор ICU, плейсхолдеры
pnpm check:analytics                        # страж реестра событий (~1с): каталоги, PII в именах свойств, владелец ключа, серверные ключи не шлются клиентом
pnpm check:audit                            # страж журнала безопасности (~1с): каталоги, запрещённые слова деталей, живой ключ пишется / planned — нет
pnpm check:visibility                       # страж правил видимости (~1с): реестр, каталоги, связь с ПДн, утечки имён полей, провайдер + канарейка у типа, Guarded у DTO
node apps/api/scripts/verify-<name>.cjs     # e2e-сьют (при запущенном API)
```

Docker-профили сайдкаров (`--profile s3|scan|voice|calls|docs|pdf|sign`), свои сборки образов (docs-editor c обязательным `-FlushDiscovery`, sign-verifier) и ловушки среды — `docs/dev_environment.md`. Env-переменные — `docs/environment_variables.md`.

## 11. Тестовые аккаунты — ДВЕ ТРОЙКИ (не смешивать)

Пароль везде `Test1234!` (сид `apps/api/scripts/seed-test-accounts.cjs`):
- **Ручные** (человек в браузере): tester1 `+77001234567` · tester2 `+77012345678` · tester3 `+77023456789`
- **Сьюта** (все verify-скрипты, константа `SUITE`): suite1 `+77009990001` · suite2 `+77009990002` · suite3 `+77009990003`

Сьют НИКОГДА не чистит БД через deleteMany (живая база!) — правила и gc-скрипты в `docs/testing_verify_suite.md`.

## 12. Serena MCP

Для навигации по коду: `find_symbol` / `get_symbols_overview` / `find_referencing_symbols` / `replace_symbol_body` — вместо чтения файлов целиком (после ориентации через graphify). Read/Edit — для коротких файлов, конфигов и когда нужен контекст вокруг кода. Serena memories — история дизайн-решений (см. §5).

## 13. Новый сервис — чек-лист

Грилл дизайна → модуль в `modules/` → Prisma-миграция → типы/Zod в shared (обе стороны провода!) → регистрация в app.module → **чек-лист движков** (в т.ч. уведомления: файл реестра + 3 каталога + `send(tx)` + `NotificationRefRegistry`; аналитика: 3–5 событий в реестре + `track(tx)` у ключевых фактов) → веб-страница по конвенциям → `verify-<name>.cjs` → обновить `docs/` (+`docs/module_graph.md` при новом ребре, +строку в `docs/README.md`). Полный плейбук — **`docs/playbook_new_service.md`**.

## 14. Дизайн-система

Перед созданием/правкой ЛЮБОГО UI — читать `DESIGN.md` (Organic Bento warm matte: фон `#eae6de`, блоки `#fafbf8`, акцент `#588cd3`, красный только для опасного, Manrope, Phosphor Light через `<Icon/>`, ровные радиусы, tick-прогресс). Витрина кита — `/dev/ui`.
