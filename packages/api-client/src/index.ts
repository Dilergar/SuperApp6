// @superapp/api-client — ТРАНСПОРТ границы API ↔ клиенты, и только он.
// Здесь живут: axios-инстанс, подстановка токена, single-flight ротация refresh,
// обработка 401 и типизированные хелперы `apiGet/apiPost/…`.
// Здесь НЕ живут: фетчеры доменов (`*-api.ts`), ключи React Query, DTO — они
// переезжают доменно, когда их попросит второй клиент.
export * from './client';
